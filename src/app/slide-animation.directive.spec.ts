import { SlideAnimationDirective } from './slide-animation.directive';

describe('SlideAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
